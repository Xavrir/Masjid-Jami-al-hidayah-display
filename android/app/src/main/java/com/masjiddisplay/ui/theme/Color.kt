package com.masjiddisplay.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    val backgroundTop = Color(0xFF0A5EB8)
    val backgroundBottom = Color(0xFF063A7A)
    val accentCyan = Color(0xFF00E5FF)
    val textPrimary = Color.White
    val textSecondary = Color.White.copy(alpha = 0.9f)
    val textMuted = Color.White.copy(alpha = 0.7f)
    val textSubtle = Color.White.copy(alpha = 0.5f)
    val highlightBorder = Color(0xFF00E5FF).copy(alpha = 0.6f)
    val highlightBackground = Color.White.copy(alpha = 0.1f)
}
